import { Service } from 'typedi';
import { Logger, LoggerService } from '../../logger/logger.service';

@Service()
export class {{firstUpperCase name}}Service {

    constructor(@Logger(__filename) private logger: LoggerService) {

    /**
     * Service example
     * @returns example
     */
    public getSomething() {
        return {
            ping: 'pong'
        };
    }

}

